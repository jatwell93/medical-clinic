---
phase: 03-demand-competitors
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - Johnston_St_v2.ipynb
  - scripts/extend_v2_notebook_phase3.py
  - scripts/validate_v2_notebook.py
  - data/cache/competitors.geojson
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Johnston_St_v2.ipynb contains a §4 Competitor Landscape section with classify_place function"
    - "classify_place function checks pharmacy brands first via displayName keyword rules (D-07), then exclude keywords, corporate GP brands, independent GP indicators, and allied health indicators (D-09)"
    - "CORPORATE_GP_BRANDS dict contains 12 keywords: ipn, myhealth, family doctor, forhealth, healius, bupa, better medical, jupiter health, ochre, sonic, qualitas, health&co"
    - "PHARMACY_BRANDS dict contains 11 keywords: chemist warehouse, priceline, amcal, terrywhite, terry white, chemmart, sigma, pharmacy 4 less, national pharmacy, soul pattinson, discount drug stores"
    - "EXCLUDE_KEYWORDS list contains 21 terms: skin, cosmetic, dermatology, laser, beauty, vet, veterinary, animal, optometry, optometrist, podiatry, audiology, radiology, imaging, pathology, laboratory, specialist, paediatric, obstetric, gynaecology, psychiatry"
    - "fuzzy_dedupe_competitors function uses rapidfuzz.fuzz.token_sort_ratio with name_threshold=85 and address_threshold=80 (D-09)"
    - "rapidfuzz is imported (from rapidfuzz import fuzz) — NOT fuzzywuzzy"
    - "Deduped competitor GeoDataFrame is persisted to data/cache/competitors.geojson (D-08)"
    - "GeoJSON load-from-cache path exists (if competitors.geojson exists and not FORCE_REFRESH, load it)"
    - "geopandas.sjoin is used for competitor-to-ring spatial assignment (which ring is each competitor in)"
    - "Per-ring competitor counts table is printed (GP clinics, pharmacies, allied health by ring)"
    - "Manual-review checkpoint prints ambiguous-rows table inline (D-11 — no input() blocking)"
    - "No input() call in any Phase 3 cell (D-11, PIPE-01)"
    - "Capacity caveat markdown cell present with text 'Places listings ≠ FTE GPs' and 'range spans clinic-derived and benchmark-derived estimates' (D-12: two-method range IS the caveat)"
    - "Folium interactive competitor map with toggleable ring layers + competitor type colours (D-15)"
    - "3 static matplotlib + contextily figures (one per ring: 1km, 3km, 5km) for PDF (D-15)"
    - "Peer competitor table with GP count, pharmacy count by brand, allied health count, GP-per-1,000 ratio (D-16)"
    - "GP-per-1,000 ratio benchmarked against VIC average 117 FTE GPs/100k (COMP-03)"
    - "No sklearn import in any Phase 3 cell (DEMAND-04, Pitfall 14)"
    - "Validator extended with Phase 3 COMP-01..03 checks"
  artifacts:
    - path: "Johnston_St_v2.ipynb"
      provides: "Notebook extended with §4 Competitor Landscape (classification, dedupe, maps, peer table)"
      contains: "classify_place"
    - path: "scripts/extend_v2_notebook_phase3.py"
      provides: "Extended generator with §4 competitor cells appended"
      contains: "competitor_cells"
    - path: "scripts/validate_v2_notebook.py"
      provides: "Validator extended with Phase 3 COMP-01..03 checks"
      contains: "COMP-01"
    - path: "data/cache/competitors.geojson"
      provides: "Persisted deduped competitor GeoDataFrame for offline re-runs (D-08)"
      contains: "FeatureCollection"
  key_links:
    - from: "Johnston_St_v2.ipynb (§1.4 Places client — site_places dict)"
      to: "Johnston_St_v2.ipynb (§4 competitor classification cell)"
      via: "site_places raw place lists flow from §1.4 to §4 classify_place + dedupe"
      pattern: "site_places"
    - from: "Johnston_St_v2.ipynb (§2 catchment buffers)"
      to: "Johnston_St_v2.ipynb (§4 ring assignment cell)"
      via: "buffers dict flows from §2 to §4 spatial join for ring assignment"
      pattern: "buffers"
    - from: "Johnston_St_v2.ipynb (§1.4 Places client — peer_places dict)"
      to: "Johnston_St_v2.ipynb (§4 peer competitor table cell)"
      via: "peer_places raw place lists flow from §1.4 to §4 peer table"
      pattern: "peer_places"
---

<objective>
Extend the notebook with §4 Competitor Landscape: classify raw Places results into categories (corporate GP, independent GP, pharmacy by brand, allied health, exclude), fuzzy-dedupe individual-practitioner listings with rapidfuzz, persist the deduped GeoDataFrame to `data/cache/competitors.geojson`, assign competitors to catchment rings via spatial join, produce per-ring counts + a peer competitor table, render an interactive folium map + 3 static matplotlib figures, and print a manual-review checkpoint for ambiguous rows.

Purpose: This is the supply side of the demand-vs-capacity gap (DEMAND-03). v1's raw `type=doctor` counts were inflated 2–5× by specialists, individual listings, and miscategorised places (Pitfall 9). The classification + fuzzy-dedupe pipeline fixes this. The GeoJSON persistence (D-08) ensures re-runs never touch the API (Pitfall 10). The per-ring counts feed the GP FTE capacity estimate in Plan 03-03. The peer competitor table (D-16) fills the DEMO-03 placeholder with real data.

Output: `Johnston_St_v2.ipynb` extended with §4 cells, `scripts/extend_v2_notebook_phase3.py` extended with `competitor_cells()`, `scripts/validate_v2_notebook.py` extended with COMP-01..03 checks, `data/cache/competitors.geojson` created (or load path present).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-demand-competitors/03-CONTEXT.md
@.planning/phases/03-demand-competitors/03-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@Johnston_St_v2.ipynb
@scripts/extend_v2_notebook_phase3.py
@scripts/validate_v2_notebook.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generator with §4 Competitor Landscape cells (classification, dedupe, GeoJSON persistence, ring assignment, maps, peer table, manual review)</name>
  <files>scripts/extend_v2_notebook_phase3.py, Johnston_St_v2.ipynb</files>
  <read_first>scripts/extend_v2_notebook_phase3.py, Johnston_St_v2.ipynb, .planning/phases/03-demand-competitors/03-RESEARCH.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md</read_first>
  <action>
Extend `scripts/extend_v2_notebook_phase3.py` with a new `competitor_cells()` function that returns the §4 cells described below. Update `main()` to call `competitor_cells()` and append them after the §1.5 cells, with cell-replacement idempotency: check for marker `"# §4 Competitor Landscape"`, if found remove existing §4 cells (up to `"# §5 Demand Model"` or `"## Next Steps"`) and re-append.

Add `%pip install -q rapidfuzz` to the §0.1 installs cell (or a new install line in the generator's BASE_ASSUMPTIONS extension section — add it as a comment in the first §4 code cell: `# pip install rapidfuzz  # MIT-licensed fuzzy string matching (not fuzzywuzzy)`).

**§4 COMPETITOR LANDSCAPE** (new cells, in order):

1. **Markdown cell** — `# §4 Competitor Landscape` — teaching commentary: v1's raw `type=doctor` Google Places counts were inflated 2–5× by specialists, individual practitioner listings (each GP gets their own Places entry plus the practice), and miscategorised places (skin clinics, vets, cosmetic clinics mis-tagged as "doctor") — Pitfall 9; v1 concluded "6.6 doctors per 1,000" when the sane metro benchmark is ~1.2 FTE GPs per 1,000; this section fixes it with keyword-based classification (D-09), rapidfuzz fuzzy-dedupe of individual-practitioner listings (D-09), and a manual-review checkpoint for ambiguous rows (D-11); the deduped GeoDataFrame is persisted to `data/cache/competitors.geojson` (D-08) so re-runs never touch the API (Pitfall 10); `rapidfuzz` is MIT-licensed (not GPL `fuzzywuzzy`).

2. **Code cell (§4.1 Classification rules)** — define `CORPORATE_GP_BRANDS`, `PHARMACY_BRANDS`, `EXCLUDE_KEYWORDS`, and `classify_place()` exactly as specified in 03-RESEARCH.md Pattern 2, with these concrete values. D-07: pharmacy brand classification via keyword rules on `displayName` (e.g. "Priceline" → Priceline, "Chemist Warehouse" → CW, "Amcal" → Amcal, "TerryWhite Chemmart" → TerryWhite, else independent). No extra Text Search API calls — the per-type `pharmacy` query catches all pharmacies in range:
   - `CORPORATE_GP_BRANDS` dict with exactly 12 keys: `"ipn": "IPN (Sonic Healthcare)"`, `"myhealth": "Amplar Health (Myhealth/Medibank)"`, `"family doctor": "Family Doctor"`, `"forhealth": "ForHealth"`, `"healius": "ForHealth (formerly Healius)"`, `"bupa": "Bupa (Partnered Health)"`, `"better medical": "Amplar Health (Better Medical)"`, `"jupiter health": "Jupiter Health"`, `"ochre": "Ochre Health"`, `"sonic": "Sonic Healthcare"`, `"qualitas": "Qualitas"`, `"health&co": "ForHealth (Health&Co)"`
   - `PHARMACY_BRANDS` dict with exactly 11 keys: `"chemist warehouse": "Chemist Warehouse"`, `"priceline": "Priceline"`, `"amcal": "Amcal"`, `"terrywhite": "TerryWhite Chemmart"`, `"terry white": "TerryWhite Chemmart"`, `"chemmart": "TerryWhite Chemmart"`, `"sigma": "Sigma (API)"`, `"pharmacy 4 less": "Pharmacy 4 Less"`, `"national pharmacy": "National Pharmacy"`, `"soul pattinson": "Soul Pattinson"`, `"discount drug stores": "Discount Drug Stores"`
   - `EXCLUDE_KEYWORDS` list with exactly 21 terms: `"skin"`, `"cosmetic"`, `"dermatology"`, `"laser"`, `"beauty"`, `"vet"`, `"veterinary"`, `"animal"`, `"optometry"`, `"optometrist"`, `"podiatry"`, `"audiology"`, `"radiology"`, `"imaging"`, `"pathology"`, `"laboratory"`, `"specialist"`, `"paediatric"`, `"obstetric"`, `"gynaecology"`, `"psychiatry"`
   - `classify_place(name, primary_type, types_list)` function: checks pharmacy brands first (returns `("pharmacy", label)`), then `primary_type == "pharmacy"` or `"pharmacy" in types_list` (returns `("pharmacy", "Independent")`), then exclude keywords (returns `("exclude", kw)`), then corporate GP brands (returns `("gp_corporate", label)`), then independent GP indicators (list: `"medical centre"`, `"medical center"`, `"general practice"`, `"gp clinic"`, `"gp practice"`, `"medical clinic"`, `"family practice"`, `"health centre"`, `"health center"`, `"community health"` — returns `("gp_independent", "Independent")`), then `primary_type in ("doctor", "medical_center", "medical_clinic")` without clinic indicator (returns `("ambiguous", "doctor-type but no clinic indicator in name")`), then allied health indicators (list: `"physio"`, `"physiotherapy"`, `"psychology"`, `"psychologist"`, `"dental"`, `"dentist"`, `"chiropractic"`, `"chiropractor"`, `"occupational therapy"`, `"dietitian"`, `"dietician"`, `"speech"`, `"acupuncture"`, `"osteopath"` — returns `("allied_health", "Allied Health")`), then `primary_type in ("physiotherapist", "dentist", "dental_clinic", "chiropractor")` (returns `("allied_health", "Allied Health")`), else returns `("ambiguous", f"Unclassified (primaryType={primary_type})")`
   - Print `[classify] classify_place() defined — 12 corporate GP brands, 11 pharmacy brands, 21 exclude keywords`

3. **Code cell (§4.2 Fuzzy-dedupe + GeoDataFrame construction + GeoJSON persistence)** — implement `fuzzy_dedupe_competitors()` exactly as specified in 03-RESEARCH.md Pattern 3, then build the competitor GeoDataFrame and persist to GeoJSON:
   - `from rapidfuzz import fuzz` (NOT fuzzywuzzy)
   - `fuzzy_dedupe_competitors(places_gdf, name_threshold=85, address_threshold=80)`: iterates rows, uses `fuzz.token_sort_ratio(name_norm, kept["name"])` and `fuzz.token_sort_ratio(addr_norm, kept["addr"])`, match if `name_sim >= name_threshold and addr_sim >= address_threshold`, returns `(deduped, duplicates)`
   - Build GeoDataFrame from `site_places` raw results: flatten all `(type, radius)` results into a list of place dicts, extract `id`, `displayName` (from `displayName.text`), `lat`/`lon` (from `location.latitude`/`longitude`), `types`, `primaryType`, `primaryTypeDisplayName` (from `primaryTypeDisplayName.text` if present), `businessStatus`, `formattedAddress`, call `classify_place()` on each row
   - Apply `fuzzy_dedupe_competitors()` to the GeoDataFrame
   - **D-08 GeoJSON persistence**: `geojson_path = CACHE_DIR / "competitors.geojson"`; if `geojson_path.exists() and not FORCE_REFRESH`: load via `gpd.read_file(geojson_path)`, print `[cache] loaded N competitors from competitors.geojson`; else: `competitors_gdf.to_file(geojson_path, driver="GeoJSON")`, print `[cache] wrote N competitors to competitors.geojson`
   - Print dedupe stats: `[dedupe] kept N, removed M duplicates`

4. **Code cell (§4.3 Ring assignment via spatial join)** — assign each competitor to a catchment ring:
   - Use `geopandas.sjoin`: reproject competitors to EPSG:7855, spatial join with buffer GeoDataFrames
   - For each radius in `BASE_ASSUMPTIONS["catchment_radii_m"]`: `comp_in_ring = gpd.sjoin(competitors_gdf_metric, ring_gdf, predicate="within")`
   - Build `per_ring_counts` DataFrame: columns = `ring_km`, `gp_corporate`, `gp_independent`, `pharmacy`, `allied_health`, `ambiguous`, `exclude`, `total`
   - Print the per-ring counts table with `print(per_ring_counts.to_string(index=False))`

5. **Code cell (§4.4 Manual-review checkpoint)** — D-11, no `input()` blocking:
   ```python
   # D-11: Manual-review checkpoint — print ambiguous rows inline (no input() blocking)
   ambiguous = competitors_gdf[competitors_gdf["category"] == "ambiguous"]
   if len(ambiguous) > 0:
       print(f"\n⚠ MANUAL REVIEW: {len(ambiguous)} ambiguous competitors flagged.")
       print("Review the table below. If any are misclassified, edit data/cache/competitors.geojson")
       print("manually and re-run from §5.\n")
       review_cols = ["displayName", "formattedAddress", "primaryType", "label"]
       print(ambiguous[review_cols].to_string(index=False))
   else:
       print("[review] No ambiguous competitors — all classified successfully.")
   ```

6. **Markdown cell** — `## §4.5 Competitor Maps` — teaching commentary: folium interactive map (inline only, NOT in PDF — D-15); 3 static matplotlib + contextily figures (one per ring, for PDF); competitors coloured by type (GP corporate=red, GP independent=orange, pharmacy=blue, allied health=green, ambiguous=gray); same pattern as Phase 2 catchment maps.

7. **Code cell (§4.5 folium competitor map)** — implement `make_competitor_map()` exactly as specified in 03-RESEARCH.md Example 5, with these concrete values:
   - `folium.Map(location=[site_lat, site_lon], zoom_start=13, tiles="CartoDB Positron")`
   - Site marker with `icon=folium.Icon(color="red", icon="star", prefix="fa")`
   - Ring layers as `FeatureGroup` per ring with `folium.Circle` (colours: `{1000: "blue", 3000: "green", 5000: "purple"}`)
   - Competitor markers as `CircleMarker` with `type_colours = {"gp_corporate": "red", "gp_independent": "orange", "pharmacy": "blue", "allied_health": "green", "ambiguous": "gray"}`
   - `folium.LayerControl(collapsed=False)`
   - Display the map

8. **Code cell (§4.5 static matplotlib figures)** — 3 figures, one per ring (1km, 3km, 5km), same pattern as Phase 2 §2.5 static figures:
   - `import matplotlib.pyplot as plt`, `import contextily as cx`
   - For each radius: plot buffer boundary, plot competitors coloured by category, add contextily basemap (`cx.add_basemap(ax, crs="EPSG:4326", source=cx.providers.CartoDB.Positron)`), set title `f"Competitors — {r//1000} km ring"`

9. **Markdown cell** — `## §4.6 Peer Competitor Table` — teaching commentary: D-16 — separate from the Phase 2 census-only peer table; this table adds GP count, pharmacy count by brand, allied health count, and GP-per-1,000 ratio per peer; benchmarked against VIC average ~117 FTE GPs/100k (COMP-03); fills the DEMO-03 placeholder GP/pharmacy columns with real Places data.

10. **Code cell (§4.6 peer competitor table)** — build the peer competitor table from `peer_places`:
    - For each peer postcode: classify all places, count by category (gp_corporate + gp_independent = GP clinic count, pharmacy by brand, allied health)
    - Compute GP-per-1,000: `(gp_clinic_count * BASE_ASSUMPTIONS["avg_fte_per_clinic"]) / (peer_pop / 1000)` using ERP-scaled population from Phase 2
    - Benchmark column: VIC average = `BASE_ASSUMPTIONS["amwac_per_100k"]` (110.4/100k planning benchmark) and 117 (VIC actual)
    - Print the table with `print(peer_comp_table.to_string(index=False))`
    - Print benchmark comparison: `f"[benchmark] VIC average: {BASE_ASSUMPTIONS['amwac_per_100k']} FTE GPs/100k (AMWAC planning), 117 (VIC actual RACGP 2025)"`

11. **Markdown cell** — `## §4.7 Capacity Caveat` — D-12: print the two-method range caveat prominently:
    > **Caveat (D-12):** Places listings ≠ FTE GPs. The GP capacity estimate (computed in §5) spans a clinic-derived method (clinic count × 4.0 FTE/clinic) and a benchmark-derived method (AMWAC 110.4/100k × population). The variance IS the caveat — there is no single point estimate.

After creating the cells, update `main()` in the generator script to call `competitor_cells()` and append after §1.5 cells. Then RUN IT: `python scripts/extend_v2_notebook_phase3.py`.
  </action>
  <verify>
    python scripts/extend_v2_notebook_phase3.py  # must run without error
    python -c "import json,sys; sys.stdout.reconfigure(encoding='utf-8'); nb=json.load(open('Johnston_St_v2.ipynb',encoding='utf-8')); s=''.join(''.join(c.get('source',[])) for c in nb['cells']); assert 'classify_place' in s, 'No classify_place'; assert 'CORPORATE_GP_BRANDS' in s, 'No CORPORATE_GP_BRANDS'; assert 'PHARMACY_BRANDS' in s, 'No PHARMACY_BRANDS'; assert 'EXCLUDE_KEYWORDS' in s, 'No EXCLUDE_KEYWORDS'; assert 'fuzzy_dedupe_competitors' in s, 'No fuzzy_dedupe'; assert 'rapidfuzz' in s, 'No rapidfuzz'; assert 'fuzzywuzzy' not in s, 'fuzzywuzzy found'; assert 'competitors.geojson' in s, 'No GeoJSON persistence'; assert 'sjoin' in s or 'spatial join' in s.lower(), 'No spatial join'; assert 'input(' not in s, 'input() found (D-11 violation)'; assert 'sklearn' not in s, 'sklearn found'; assert 'make_competitor_map' in s, 'No competitor map'; assert 'cx.add_basemap' in s, 'No contextily basemap'; assert 'gp_per_1000' in s or 'GP-per-1,000' in s or 'gp_per_1k' in s, 'No GP-per-1000 ratio'; print('All §4 checks passed')"
  </verify>
  <acceptance_criteria>
    - Johnston_St_v2.ipynb contains "classify_place" function (D-09 classification)
    - Johnston_St_v2.ipynb contains "CORPORATE_GP_BRANDS" dict with "ipn", "myhealth", "family doctor", "forhealth", "healius", "bupa", "better medical", "jupiter health", "ochre", "sonic", "qualitas", "health&co" (12 keywords)
    - Johnston_St_v2.ipynb contains "PHARMACY_BRANDS" dict with "chemist warehouse", "priceline", "amcal", "terrywhite", "chemmart", "sigma", "pharmacy 4 less", "national pharmacy", "soul pattinson", "discount drug stores" (11 keywords)
    - Johnston_St_v2.ipynb contains "EXCLUDE_KEYWORDS" list with "skin", "cosmetic", "dermatology", "vet", "optometry", "podiatry", "radiology", "pathology", "specialist", "psychiatry" (subset of 21 terms verifiable)
    - Johnston_St_v2.ipynb contains "fuzzy_dedupe_competitors" function (D-09 fuzzy dedupe)
    - Johnston_St_v2.ipynb contains "from rapidfuzz import fuzz" (MIT-licensed, not fuzzywuzzy)
    - Johnston_St_v2.ipynb does NOT contain "fuzzywuzzy" (GPL, unmaintained — forbidden)
    - Johnston_St_v2.ipynb contains "token_sort_ratio" (rapidfuzz API)
    - Johnston_St_v2.ipynb contains "name_threshold=85" and "address_threshold=80" (D-09 thresholds)
    - Johnston_St_v2.ipynb contains "competitors.geojson" (D-08 GeoJSON persistence)
    - Johnston_St_v2.ipynb contains "to_file" and "driver=\"GeoJSON\"" or "driver='GeoJSON'" (GeoDataFrame persistence)
    - Johnston_St_v2.ipynb contains "gpd.read_file" for cache load path (D-08 load-from-cache)
    - Johnston_St_v2.ipynb contains "sjoin" (spatial join for ring assignment)
    - Johnston_St_v2.ipynb does NOT contain "input(" (D-11 — no blocking input() calls)
    - Johnston_St_v2.ipynb contains "MANUAL REVIEW" or "manual review" (D-11 checkpoint)
    - Johnston_St_v2.ipynb contains "make_competitor_map" (D-15 folium map)
    - Johnston_St_v2.ipynb contains "folium.FeatureGroup" (toggleable ring layers, D-15)
    - Johnston_St_v2.ipynb contains "folium.LayerControl" (layer control)
    - Johnston_St_v2.ipynb contains "cx.add_basemap" (static figures with contextily, D-15)
    - Johnston_St_v2.ipynb contains "gp_per_1000" or "GP-per-1,000" or "gp_per_1k" (COMP-03 ratio)
    - Johnston_St_v2.ipynb contains "117" (VIC actual FTE GPs/100k benchmark, COMP-03)
    - Johnston_St_v2.ipynb contains "Places listings" and "FTE GPs" (D-12 capacity caveat)
    - Johnston_St_v2.ipynb does NOT contain "sklearn" (DEMAND-04)
    - scripts/extend_v2_notebook_phase3.py contains "competitor_cells" function
    - scripts/extend_v2_notebook_phase3.py is idempotent (contains marker check for "# §4 Competitor Landscape")
  </acceptance_criteria>
  <done>All acceptance criteria met — §4 cells generated, classification/dedupe/maps/peer table/caveat all present, no fuzzywuzzy/sklearn/input()</done>
</task>

<task type="auto">
  <name>Task 2: Extend validator with Phase 3 COMP-01..03 checks</name>
  <files>scripts/validate_v2_notebook.py</files>
  <read_first>scripts/validate_v2_notebook.py, Johnston_St_v2.ipynb</read_first>
  <action>
Extend `scripts/validate_v2_notebook.py` with Phase 3 competitor checks. Add a new section after the Phase 2 demographics checks (after the `passes.extend(phase2_demog_passes)` line) with these checks:

**COMP-01 (Places API New with saturation + dedupe):**
```python
comp01_url = "places.googleapis.com/v1/places:searchNearby" in all_source
comp01_fieldmask = "X-Goog-FieldMask" in all_source
comp01_saturated = "places_nearby_saturated" in all_source
comp01_dedupe = "place.id" in all_source or "place_id" in all_source
comp01_no_rating = "places.rating" not in all_source and "places.userRatingCount" not in all_source
comp01 = comp01_url and comp01_fieldmask and comp01_saturated and comp01_dedupe and comp01_no_rating
```

**COMP-02 (pharmacy brand classification + clinic-vs-practitioner filtering):**
```python
comp02_pharmacy = "PHARMACY_BRANDS" in all_source and "chemist warehouse" in all_source.lower()
comp02_classify = "classify_place" in all_source
comp02_fuzzy = "fuzzy_dedupe_competitors" in all_source and "rapidfuzz" in all_source
comp02_no_fuzzywuzzy = "fuzzywuzzy" not in all_source
comp02 = comp02_pharmacy and comp02_classify and comp02_fuzzy and comp02_no_fuzzywuzzy
```

**COMP-03 (competitor inventory table + map layers + GP-per-1,000 benchmark):**
```python
comp03_geojson = "competitors.geojson" in all_source
comp03_folium = "folium.FeatureGroup" in all_source and "folium.LayerControl" in all_source
comp03_static = "cx.add_basemap" in all_source
comp03_ratio = "gp_per_1000" in all_source or "GP-per-1,000" in all_source or "gp_per_1k" in all_source
comp03_benchmark = "117" in all_source
comp03 = comp03_geojson and comp03_folium and comp03_static and comp03_ratio and comp03_benchmark
```

**DEMAND-04 (no ML — negative assertion):**
```python
demand04_no_sklearn = "sklearn" not in all_source and "scikit-learn" not in all_source
demand04 = demand04_no_sklearn
```

Add these checks using the same `check_phase3()` helper pattern (or inline with `phase3_passes`/`phase3_failures` lists). Add the Phase 3 labels to the `report()` function's label list so they print in the summary. Add `"COMP-01"`, `"COMP-02"`, `"COMP-03"`, `"DEMAND-04"` to the structural-failure filter list at the end of `report()`.

Run: `python scripts/validate_v2_notebook.py` — must output OVERALL: PASS with all Phase 3 checks passing.
  </action>
  <verify>python scripts/validate_v2_notebook.py  # must exit 0 with OVERALL: PASS including Phase 3 COMP-01..03 + DEMAND-04 checks</verify>
  <acceptance_criteria>
    - scripts/validate_v2_notebook.py contains "COMP-01" check (Places URL + field mask + saturation + dedupe + no rating)
    - scripts/validate_v2_notebook.py contains "COMP-02" check (pharmacy brands + classify_place + rapidfuzz + no fuzzywuzzy)
    - scripts/validate_v2_notebook.py contains "COMP-03" check (GeoJSON + folium layers + contextily + GP-per-1000 + 117 benchmark)
    - scripts/validate_v2_notebook.py contains "DEMAND-04" check (no sklearn negative assertion)
    - `python scripts/validate_v2_notebook.py` exits 0 (OVERALL: PASS)
    - Validator output includes "Phase 3 COMP-01", "Phase 3 COMP-02", "Phase 3 COMP-03", "Phase 3 DEMAND-04" labels
  </acceptance_criteria>
  <done>Validator extended with COMP-01..03 + DEMAND-04 checks, all pass, OVERALL: PASS</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python scripts/extend_v2_notebook_phase3.py` runs without error and appends §4 cells
- [ ] `python scripts/validate_v2_notebook.py` exits 0 (OVERALL: PASS including new Phase 3 checks)
- [ ] No `fuzzywuzzy` import (use rapidfuzz only)
- [ ] No `sklearn` import (DEMAND-04)
- [ ] No `input()` call in any Phase 3 cell (D-11)
- [ ] `competitors.geojson` persistence path present (D-08)
- [ ] 12 corporate GP brands + 11 pharmacy brands + 21 exclude keywords in classify_place
- [ ] Folium map with FeatureGroup layers + 3 static contextily figures (D-15)
- [ ] Peer competitor table with GP-per-1,000 benchmarked against 117 VIC (COMP-03)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- §4 Competitor Landscape fully implemented (classification, dedupe, GeoJSON, maps, peer table, manual review, caveat)
- Validator extended and passing with Phase 3 COMP-01..03 + DEMAND-04 checks
</success_criteria>

<output>
After completion, create `.planning/phases/03-demand-competitors/03-02-SUMMARY.md`
</output>
