---
phase: 01-scaffolding-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - data/local/
  - data/cache/.gitkeep
  - outputs/.gitkeep
  - docs/abs-api/
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - ".gitignore exists and contains .env, data/local/, outputs/, data/cache/redirects.sqlite"
    - ".env.example exists with GOOGLE_PLACES_KEY= placeholder and usage instructions"
    - "data/local/ contains the 8 moved data files (zips, xlsx, pdfs)"
    - "data/cache/ directory exists (with .gitkeep so it survives in git)"
    - "outputs/ directory exists (with .gitkeep)"
    - "Repo root is clean — no .zip, .xlsx, or .pdf files remain at root level (except within .planning/)"
    - "ABS API reference markdown files moved to docs/abs-api/"
  artifacts:
    - path: ".gitignore"
      provides: "Repo hygiene rules — secrets, local data, outputs, cache metadata"
      contains: ".env"
    - path: ".env.example"
      provides: "Documented key name for local Windows runs"
      contains: "GOOGLE_PLACES_KEY"
    - path: "data/local/"
      provides: "Fallback data assets for offline/local runs (D-13, D-14)"
    - path: "data/cache/.gitkeep"
      provides: "Cache directory exists in git for committed API response caches (D-12)"
    - path: "docs/abs-api/"
      provides: "ABS Data API reference documentation moved from repo root"
  key_links:
    - from: ".gitignore"
      to: "data/cache/"
      via: "gitignore excludes redirects.sqlite but NOT *.json (D-12: commit API caches)"
      pattern: "data/cache/redirects\\.sqlite"
---

<objective>
Scaffold the repository structure: .gitignore, .env.example, directory layout, and relocate existing data files from the repo root into `data/local/` and ABS reference docs into `docs/abs-api/`.

Purpose: Establish the clean repo layout defined in ARCHITECTURE.md's "Recommended Project Structure" so the notebook (Plan 01-02) can reference `data/local/`, `data/cache/`, and `outputs/` paths deterministically. This is the reproducibility keystone — caches committed to git make the notebook runnable by anyone with zero API keys (PIPE-04).

Output: `.gitignore`, `.env.example`, `data/local/` (8 moved files), `data/cache/.gitkeep`, `outputs/.gitkeep`, `docs/abs-api/` (4 moved files), clean repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-data-pipeline/01-CONTEXT.md
@.planning/phases/01-scaffolding-data-pipeline/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore and .env.example</name>
  <files>.gitignore, .env.example</files>
  <read_first>.planning/research/ARCHITECTURE.md, .planning/phases/01-scaffolding-data-pipeline/01-CONTEXT.md, .planning/phases/01-scaffolding-data-pipeline/01-RESEARCH.md</read_first>
  <action>
Create `.gitignore` with EXACTLY this content (per RESEARCH.md §6, D-12, D-13, D-15):

```
# Secrets — never commit (D-15)
.env

# Python
__pycache__/
*.pyc
*.pyo

# Jupyter
.ipynb_checkpoints/

# Local fallback data — large files, not in git (D-13)
data/local/

# Generated outputs — maps, report, PDF (regenerated on run)
outputs/

# Cache metadata — regenerable; keep the *.json response files (D-12)
data/cache/redirects.sqlite

# OS
.DS_Store
Thumbs.db
```

Key policy: `data/cache/` is NOT gitignored (only `data/cache/redirects.sqlite` is). API response JSON files ARE committed per D-12 — they contain no secrets and are the reproducibility keystone.

Create `.env.example` with EXACTLY this content:
```
# Local Windows development — copy to .env and fill in your key.
# In Colab, this file is not used; add the key via the 🔑 sidebar (Colab Secrets).
# Get a key: https://console.cloud.google.com/google/maps-apis/credentials
GOOGLE_PLACES_KEY=
```

The `.env.example` must have `GOOGLE_PLACES_KEY=` with no value (just the key name and an equals sign). Include the comment block explaining both environments.
  </action>
  <verify>
Run: `test -f .gitignore && test -f .env.example && grep -q "^\.env$" .gitignore && grep -q "data/local/" .gitignore && grep -q "data/cache/redirects.sqlite" .gitignore && grep -q "GOOGLE_PLACES_KEY=" .env.example && echo "PASS"`
  </verify>
  <acceptance_criteria>
    - .gitignore contains the line `.env` (exact, not `.env*`)
    - .gitignore contains `data/local/`
    - .gitignore contains `outputs/`
    - .gitignore contains `data/cache/redirects.sqlite`
    - .gitignore does NOT contain `data/cache/` (the whole directory must NOT be ignored — only redirects.sqlite)
    - .env.example contains `GOOGLE_PLACES_KEY=`
    - .env.example does NOT contain an actual key value (empty after =)
  </acceptance_criteria>
  <done>.gitignore and .env.example created with correct content; .env is gitignored, data/cache/*.json is NOT gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Create directory structure and relocate data files</name>
  <files>data/local/, data/cache/.gitkeep, outputs/.gitkeep, docs/abs-api/</files>
  <read_first>.planning/phases/01-scaffolding-data-pipeline/01-CONTEXT.md, .planning/phases/01-scaffolding-data-pipeline/01-RESEARCH.md</read_first>
  <action>
1. Create directories: `data/local/`, `data/cache/`, `outputs/`, `docs/abs-api/`
2. Create `.gitkeep` files in `data/cache/` and `outputs/` so the empty directories survive in git (data/local/ is gitignored so no .gitkeep needed there, but create the directory so files can be moved into it).

3. Move these 8 files from repo root to `data/local/` (per D-14, RESEARCH.md §7):
   - `2021_GCP_POA_for_VIC_short-header.zip` → `data/local/`
   - `POA_2021_AUST_GDA2020_SHP.zip` → `data/local/`
   - `GCP_POA3067.xlsx` → `data/local/`
   - `SA3_2021_AUST.xlsx` → `data/local/`
   - `medicare-annual-statistics-state-and-territory-2009-10-to-2024-25.xlsx` → `data/local/`
   - `medicare-quarterly-statistics-primary-care-service-type-summary-march-quarter-2025-26(1).xlsx` → `data/local/`
   - `Catchment-Report-Cohealth-collingwood-Centre-2026-6-11-113449 (1).pdf` → `data/local/`
   - `292 Johnston Ground Floor Plan (1).pdf` → `data/local/`

4. Move these 4 ABS reference markdown files from repo root to `docs/abs-api/`:
   - `Using_the_API_Australian_Bureau_of_Statistics.md` → `docs/abs-api/`
   - `Worked_Examples_Australian_Bureau_of_Statistics.md` → `docs/abs-api/`
   - `metadata.md` → `docs/abs-api/`
   - `tips.md` → `docs/abs-api/`

5. Use `git mv` for tracked files so git history is preserved. For untracked files, use regular `mv`.

Files that STAY in repo root (do NOT move): `Johnston_St_v1.ipynb`, `johnston_st_v1.py`, `CLAUDE.md`, `.planning/`, `.gitignore`, `.env.example`, `.env` (if exists).
  </action>
  <verify>
Run: `ls data/local/ | wc -l` (expect 8) && `ls docs/abs-api/ | wc -l` (expect 4) && `test -f data/cache/.gitkeep && test -f outputs/.gitkeep && echo "PASS"`
Then verify repo root is clean: `ls *.zip *.xlsx *.pdf 2>/dev/null | wc -l` (expect 0)
  </verify>
  <acceptance_criteria>
    - data/local/ contains exactly 8 files (the zips, xlsx, and pdfs listed above)
    - docs/abs-api/ contains exactly 4 markdown files
    - data/cache/.gitkeep exists
    - outputs/.gitkeep exists
    - No .zip, .xlsx, or .pdf files remain at repo root level
    - Johnston_St_v1.ipynb and johnston_st_v1.py remain at repo root (frozen reference)
  </acceptance_criteria>
  <done>All 8 data files moved to data/local/, 4 ABS docs moved to docs/abs-api/, directory structure created with .gitkeep files, repo root is clean.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f .gitignore` passes
- [ ] `test -f .env.example` passes
- [ ] `grep -c "^\.env$" .gitignore` returns 1
- [ ] `ls data/local/ | wc -l` returns 8
- [ ] `ls docs/abs-api/ | wc -l` returns 4
- [ ] `test -f data/cache/.gitkeep && test -f outputs/.gitkeep` passes
- [ ] `ls *.zip *.xlsx *.pdf 2>/dev/null | wc -l` returns 0 (repo root clean)
- [ ] `git status` shows moved files as renames, not delete+add
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- .gitignore correctly excludes .env, data/local/, outputs/, redirects.sqlite but NOT data/cache/*.json
- .env.example documents the key name for both environments
- Directory structure matches ARCHITECTURE.md's "Recommended Project Structure"
- All existing data files relocated; repo root is clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-data-pipeline/01-01-SUMMARY.md`
</output>
