---
phase: 05-scenarios-executive-report
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - Johnston_St_v2.ipynb
  - scripts/extend_v2_notebook_phase5.py
  - outputs/Johnston_St_v2_Executive_Report.pdf
  - outputs/Johnston_St_v2_Executive_Report.html
autonomous: true
requirements:
  - REP-01
  - REP-02
  - REP-03
  - REP-04

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Johnston_St_v2.ipynb contains a §8 Executive Report section that renders the report{} dict into a Jinja2 HTML template"
    - "The Jinja2 template produces an 8-section narrative report: (1) Executive Summary, (2) Site & Catchment, (3) Competitor Landscape, (4) Financial Model, (5) Scenarios & Sensitivity, (6) Pharmacy Synergy, (7) Assumptions Register, (8) Data Sources & Citations (D-14)"
    - "Page 1 of the report contains the go/no-go verdict + 5 key numbers (steady-state EBITDA, margin, peak capital, operating breakeven month, payback period) + one catchment map (D-13)"
    - "The go/no-go verdict is BINARY (GO or NO-GO) — no CONDITIONAL tier (D-03)"
    - "The verdict gate is steady-state annual EBITDA positive AND margin > 15% (D-01); payback is reported as context, not the gate"
    - "The verdict is 100% standalone — the verdict text never mentions the pharmacy (D-04, Core Value)"
    - "Flip-conditions are presented as tornado zero-crossings — one sentence per lever (D-02)"
    - "The assumptions register is a 5-column table: Parameter | Value | Source | Date | Confidence (D-16, REP-01)"
    - "Every BASE_ASSUMPTIONS key appears in the assumptions register (REP-01)"
    - "Data sources are cited with footnote-style [1], [2] markers in the prose + a footnotes section with access dates (D-17, REP-04)"
    - "The PDF is generated via weasyprint from the Jinja2 HTML (D-15, REP-03)"
    - "PDF generation is Colab-only — local Windows runs produce HTML only with a printed note (D-18)"
    - "The §8 render step is re-runnable standalone — re-running §8 alone regenerates the PDF from current report{} state (ARCHITECTURE.md Data Flow #4)"
    - "All static figures (catchment maps, competitor maps, ramp chart, 2 tornado charts, billing-mix curve) are embedded in the PDF — no folium maps (D-15, Pitfall 16)"
    - "scripts/extend_v2_notebook_phase5.py is updated to add §8 cells (idempotent marker '# §8 Executive Report')"
  artifacts:
    - path: "Johnston_St_v2.ipynb"
      provides: "Notebook extended with §8 Executive Report (Jinja2 template + weasyprint PDF + assumptions register + verdict + citations)"
      contains: "verdict ="
    - path: "scripts/extend_v2_notebook_phase5.py"
      provides: "Phase 5 generator updated with §8 cells (idempotent cell-replacement on '# §8 Executive Report' marker)"
      contains: "json.dump"
    - path: "outputs/Johnston_St_v2_Executive_Report.html"
      provides: "HTML executive report (always produced — local Windows fallback)"
      contains: ""
    - path: "outputs/Johnston_St_v2_Executive_Report.pdf"
      provides: "PDF executive report (Colab-only — weasyprint + GTK)"
      contains: ""
  key_links:
    - from: "Johnston_St_v2.ipynb (§7 report{} deposits from Plan 05-01 + §6 report{} deposits from Phase 4)"
      to: "Johnston_St_v2.ipynb (§8 Jinja2 template render)"
      via: "§8 reads the full report{} dict and renders it into HTML → weasyprint PDF (ARCHITECTURE.md Pattern 5, Data Flow #4)"
      pattern: "template.render(report=report"
    - from: "Johnston_St_v2.ipynb (§8 verdict logic)"
      to: "Johnston_St_v2.ipynb (report{} dict)"
      via: "verdict string + 5 key numbers + flip-conditions deposited as report['verdict'], report['key_numbers'], report['flip_conditions']"
      pattern: "report["
---

<objective>
Add §8 Executive Report to the notebook: the go/no-go verdict logic (D-01..D-04), the 5-column assumptions register (REP-01, D-16), the 8-section Jinja2 HTML template (D-14, D-15), weasyprint PDF generation (REP-03, D-18), and footnote-style citations with access dates (REP-04, D-17). §8 is a pure render step — it reads the `report{}` accumulator (populated by §6 and §7) and the saved PNG figures, and produces the investor deliverable.

Purpose: This is THE deliverable. Every prior phase exists to make this report defensible. Page 1 carries the verdict + 5 key numbers + one map (D-13) — investors read page 1 only (Pitfall 16). The verdict is binary GO/NO-GO (D-03), 100% standalone (D-04), gated on steady-state EBITDA + margin (D-01), with tornado zero-crossings as the flip-conditions (D-02). The assumptions register (REP-01) and citations (REP-04) are the credibility layer — every constant has a source, date, and confidence level.

Output: `scripts/extend_v2_notebook_phase5.py` updated with §8 cells, `Johnston_St_v2.ipynb` extended with §8, `outputs/Johnston_St_v2_Executive_Report.html` (always), `outputs/Johnston_St_v2_Executive_Report.pdf` (Colab-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scenarios-executive-report/05-CONTEXT.md
@.planning/phases/05-scenarios-executive-report/05-01-PLAN.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@Johnston_St_v2.ipynb
@scripts/extend_v2_notebook_phase5.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add §8 Executive Report cells (verdict + assumptions register + Jinja2 template + weasyprint PDF) to the Phase 5 generator</name>
  <files>scripts/extend_v2_notebook_phase5.py, Johnston_St_v2.ipynb</files>
  <read_first>scripts/extend_v2_notebook_phase5.py, Johnston_St_v2.ipynb, .planning/phases/05-scenarios-executive-report/05-CONTEXT.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md, .planning/research/FEATURES.md</read_first>
  <action>
Update `scripts/extend_v2_notebook_phase5.py` (created in Plan 05-01) to add the §8 Executive Report cells. The generator's cell-replacement idempotency must now handle TWO markers: `"# §7 Scenarios & Sensitivity"` (Plan 05-01) and `"# §8 Executive Report"` (this plan). The cleanest approach: the §7+§8 cells are produced by a single `phase5_report_cells()` function that returns BOTH §7 and §8 cells, and the idempotency replaces from the `"# §7 Scenarios & Sensitivity"` marker to end-of-notebook with the full §7+§8 cell list. This keeps §7 and §8 in sync (re-running the generator always regenerates both). If the §7 marker is absent (fresh install), append §7+§8 at the end (after removing the §6 Next Steps cell, per Plan 05-01).

**§8 EXECUTIVE REPORT cells** (appended after the §7 cells in `phase5_report_cells()`):

**Cell 1 — Markdown header + teaching commentary:**
`# §8 Executive Report` — teaching commentary: §8 is a pure render step (ARCHITECTURE.md Pattern 5, Data Flow #4) — it reads the `report{}` accumulator (populated by §6 and §7) and the saved PNG figures, and produces the investor deliverable. Re-running §8 alone regenerates the PDF from current state. The report has 8 sections (D-14): (1) Executive Summary with verdict + 5 key numbers + one map (D-13); (2) Site & Catchment; (3) Competitor Landscape; (4) Financial Model; (5) Scenarios & Sensitivity; (6) Pharmacy Synergy (secondary upside, after the verdict); (7) Assumptions Register (5-column: Parameter | Value | Source | Date | Confidence, D-16); (8) Data Sources & Citations (footnote-style [1], [2] with access dates, D-17). The verdict is binary GO/NO-GO (D-03), 100% standalone (D-04), gated on steady-state EBITDA > 0 AND margin > 15% (D-01). PDF generation is Colab-only via weasyprint (D-18); local Windows runs produce HTML only.

**Cell 2 — Code: verdict logic (D-01, D-02, D-03, D-04, REP-02):**
Compute the binary verdict from `report{}` deposits. The gate is steady-state EBITDA > 0 AND margin > 15%. Payback is context, not the gate. Flip-conditions come from `report["tornado_zero_crossings"]`. The verdict text NEVER mentions the pharmacy (D-04).

```python
# §8.1 Go/no-go verdict logic (D-01, D-02, D-03, D-04, REP-02)
# Gate: steady-state EBITDA > 0 AND margin > 15% (D-01).
# Payback is CONTEXT, not the gate — clinic is a long-lived asset (D-01).
# Binary verdict (D-03). 100% standalone — verdict text never mentions pharmacy (D-04).

steady_ebitda = report["steady_state_ebitda"]
steady_margin = report["steady_state_margin"]
peak_capital = report["peak_capital"]
be_operating = report["breakeven_operating"]
be_payback = report["breakeven_payback"]

GATE_MARGIN = 0.15  # D-01 — margin threshold
verdict = "GO" if (steady_ebitda > 0 and steady_margin > GATE_MARGIN) else "NO-GO"

# Flip-conditions (D-02) — one sentence per lever from tornado zero-crossings
zc = report.get("tornado_zero_crossings", {})
flip_conditions = []
if zc.get("rent_yr") is not None:
    flip_conditions.append(f"Rent above ${zc['rent_yr']:,.0f}/yr flips EBITDA negative")
if zc.get("utilisation") is not None:
    flip_conditions.append(f"Utilisation below {zc['utilisation']:.1%} flips EBITDA negative")
if zc.get("gp_revenue_share") is not None:
    flip_conditions.append(f"Service-fee % below {zc['gp_revenue_share']:.1%} flips EBITDA negative")

# 5 key numbers for page 1 (D-13)
key_numbers = {
    "steady_state_ebitda": steady_ebitda,
    "steady_state_margin": steady_margin,
    "peak_capital": peak_capital,
    "breakeven_operating": be_operating,
    "breakeven_payback": be_payback,
}

# Verdict text — 100% standalone, never mentions pharmacy (D-04)
payback_str = f"month {be_payback}" if be_payback is not None else "beyond 36 months"
verdict_text = (
    f"VERDICT: {verdict}. The clinic is projected to generate ${steady_ebitda:,.0f}/yr "
    f"steady-state EBITDA at a {steady_margin:.1%} margin, requiring ${peak_capital:,.0f} "
    f"in peak startup capital. Operating breakeven is reached at month {be_operating}; "
    f"payback at {payback_str}. The verdict is robust to the tested sensitivity ranges — "
    f"EBITDA stays positive unless: " + "; ".join(flip_conditions) + "."
)

print("=" * 70)
print("§8.1 GO/NO-GO VERDICT (D-01, D-03, D-04)")
print("=" * 70)
print(verdict_text)
print("=" * 70)

# Deposit verdict + key numbers + flip-conditions for the template render
report["verdict"] = verdict
report["verdict_text"] = verdict_text
report["key_numbers"] = key_numbers
report["flip_conditions"] = flip_conditions
print(f"[report] §8.1 deposited verdict, key_numbers, flip_conditions")
```

**Cell 3 — Code: assumptions register (REP-01, D-16):**
Build a 5-column table (Parameter | Value | Source | Date | Confidence) from `BASE_ASSUMPTIONS`. Each key's citation comment (the `# source — date` inline comment from PIPE-05) is parsed into Source/Date/Confidence. For keys without a citation comment, mark Source as "TBD — needs citation" and Confidence as "LOW". Sort by section (site, demand, financial). Deposit `assumptions_register` (the DataFrame) into `report{}`.

```python
# §8.2 Assumptions register (REP-01, D-16) — 5-column table from BASE_ASSUMPTIONS
# Parameter | Value | Source | Date | Confidence (HIGH/MEDIUM/LOW)
# Parses the # source — date inline comments (PIPE-05) from the §0 BASE_ASSUMPTIONS cell.
import re

# Read the BASE_ASSUMPTIONS source from the notebook to extract citation comments
import json as _json
_nb = _json.loads((Path.cwd() / "Johnston_St_v2.ipynb").read_text(encoding="utf-8"))
_base_src = ""
for _c in _nb["cells"]:
    if _c["cell_type"] == "code":
        _s = "".join(_c.get("source", []))
        if "BASE_ASSUMPTIONS = {" in _s:
            _base_src = _s
            break

# Parse each line: "key": value,  # source — date [confidence]
register_rows = []
for line in _base_src.splitlines():
    # Match lines like:  "key":   value,  # comment
    m = re.match(r'\s*"([^"]+)":\s*(.+?),?\s*(?:#\s*(.*))?$', line)
    if not m or m.group(1) in ("site_address", "catchment_radii_m", "peer_postcodes"):
        continue  # skip non-numeric config keys
    key, val_str, comment = m.group(1), m.group(2), m.group(3) or ""
    # Extract confidence from comment
    conf = "MEDIUM"
    if "HIGH" in comment.upper():
        conf = "HIGH"
    elif "LOW" in comment.upper() or "UNCONFIRMED" in comment.upper():
        conf = "LOW"
    # Extract date (4-digit year or YYYY-MM)
    date_m = re.search(r'(20\d{2}(?:-\d{2})?|1 Jul 20\d{2})', comment)
    date = date_m.group(1) if date_m else "—"
    # Source = comment minus date/confidence tokens
    source = re.sub(r'\b(HIGH|MEDIUM|LOW|UNCONFIRMED)\b', '', comment, flags=re.IGNORECASE)
    source = re.sub(r'\b(20\d{2}(?:-\d{2})?|1 Jul 20\d{2})\b', '', source).strip(' —-')
    source = source or "TBD — needs citation"
    register_rows.append({"Parameter": key, "Value": val_str.strip(), "Source": source, "Date": date, "Confidence": conf})

assumptions_register = pd.DataFrame(register_rows)
print("=" * 90)
print("§8.2 ASSUMPTIONS REGISTER (REP-01, D-16)")
print("=" * 90)
print(assumptions_register.to_string(index=False))
print(f"\nTotal: {len(register_rows)} parameters | Confidence: "
      f"{sum(1 for r in register_rows if r['Confidence']=='HIGH')} HIGH, "
      f"{sum(1 for r in register_rows if r['Confidence']=='MEDIUM')} MEDIUM, "
      f"{sum(1 for r in register_rows if r['Confidence']=='LOW')} LOW")
print("=" * 90)
report["assumptions_register"] = assumptions_register
print(f"[report] §8.2 deposited assumptions_register ({len(register_rows)} rows)")
```

**Cell 4 — Code: Jinja2 HTML template (D-14, D-15, REP-02, REP-03):**
Define the Jinja2 template as a Python string (keeps the notebook self-contained — no external template file dependency, per CONTEXT.md D-15 planner's call). The template has 8 sections, embeds the saved PNG figures as base64 (so the HTML is portable and weasyprint can render it), uses footnote-style citations [1], [2], and has CSS for an investor-grade paginated PDF (cover page, page breaks, headers, page numbers). Render with `jinja2.Template(template).render(report=report, ...)`.

```python
# §8.3 Jinja2 HTML template (D-14, D-15, REP-02, REP-03)
# 8 sections (D-14). Page 1 = verdict + 5 key numbers + one map (D-13).
# Footnote-style citations [1], [2] with access dates (D-17, REP-04).
# CSS for investor-grade paginated PDF (weasyprint).
import base64
from datetime import date as _date
from jinja2 import Template

def _img_b64(path):
    """Embed a PNG as base64 for portable HTML + weasyprint rendering."""
    p = OUTPUTS / path if not str(path).startswith(str(OUTPUTS)) else Path(path)
    if not p.exists():
        return ""  # missing figure — render empty (don't crash the report)
    return "data:image/png;base64," + base64.b64encode(p.read_bytes()).decode()

# Figures for the report (all static matplotlib PNGs — no folium, Pitfall 16)
figs = {
    "catchment_3km": _img_b64("catchment_3km.png"),       # page 1 map (D-13)
    "ramp_cashflow": _img_b64("ramp_cashflow.png"),        # §4 Financial Model
    "tornado_ebitda": _img_b64("tornado_ebitda.png"),      # §5 Scenarios
    "tornado_peak_capital": _img_b64("tornado_peak_capital.png"),
    "billing_mix_curve": _img_b64("billing_mix_curve.png"),
}

report_html_template = r"""
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<style>
  @page { size: A4; margin: 2cm 2.5cm; @bottom-center { content: counter(page) " / " counter(pages); font-size: 9px; color: #666; } }
  @page :first { margin: 0; @bottom-center { content: none; } }
  body { font-family: 'Helvetica', 'Arial', sans-serif; font-size: 11px; line-height: 1.5; color: #222; }
  .cover { page-break-after: always; padding: 4cm 3cm; text-align: center; }
  .cover h1 { font-size: 28px; margin-bottom: 0.2cm; }
  .cover h2 { font-size: 16px; font-weight: normal; color: #555; margin-top: 0; }
  .verdict-go { color: #1a7a1a; font-size: 22px; font-weight: bold; }
  .verdict-nogo { color: #b00000; font-size: 22px; font-weight: bold; }
  .key-numbers { display: table; width: 100%; margin: 1cm 0; }
  .key-numbers div { display: table-cell; text-align: center; padding: 0.3cm; border: 1px solid #ddd; }
  .key-numbers .num { font-size: 18px; font-weight: bold; }
  .key-numbers .lbl { font-size: 9px; color: #666; }
  table { width: 100%; border-collapse: collapse; margin: 0.5cm 0; font-size: 10px; }
  th, td { border: 1px solid #ccc; padding: 4px 6px; text-align: left; }
  th { background: #f0f0f0; }
  h2 { font-size: 15px; border-bottom: 2px solid #333; padding-bottom: 2px; page-break-after: avoid; }
  h3 { font-size: 12px; color: #444; page-break-after: avoid; }
  img { max-width: 100%; page-break-inside: avoid; }
  .footnote { font-size: 8px; color: #666; border-top: 1px solid #ccc; margin-top: 1cm; padding-top: 0.3cm; }
  .secondary { background: #fffbe6; border-left: 4px solid #d4a000; padding: 0.3cm 0.5cm; }
</style>
</head>
<body>

<!-- COVER / PAGE 1 (D-13) -->
<div class="cover">
  <h1>Johnston St Medical Clinic</h1>
  <h2>Feasibility Study — Executive Report</h2>
  <h2>292-296 Johnston St, Abbotsford VIC 3067 · {{ report_date }}</h2>
  <p style="margin-top: 2cm;">
    <span class="{{ 'verdict-go' if report['verdict'] == 'GO' else 'verdict-nogo' }}">
      VERDICT: {{ report['verdict'] }}
    </span>
  </p>
  <div class="key-numbers">
    {% set kn = report['key_numbers'] %}
    <div><div class="num">${{ '%,.0f'|format(kn['steady_state_ebitda']) }}</div><div class="lbl">Steady-state EBITDA</div></div>
    <div><div class="num">{{ '%.1f'|format(kn['steady_state_margin']*100) }}%</div><div class="lbl">Margin</div></div>
    <div><div class="num">${{ '%,.0f'|format(kn['peak_capital']) }}</div><div class="lbl">Peak capital</div></div>
    <div><div class="num">mo {{ kn['breakeven_operating'] }}</div><div class="lbl">Operating breakeven</div></div>
    <div><div class="num">{{ 'mo ' ~ kn['breakeven_payback'] if kn['breakeven_payback'] else '>36 mo' }}</div><div class="lbl">Payback</div></div>
  </div>
  <p style="font-size: 10px; text-align: left; margin: 1cm 2cm;">{{ report['verdict_text'] }}</p>
  {% if figs['catchment_3km'] %}<img src="{{ figs['catchment_3km'] }}" style="max-height: 8cm;"/>{% endif %}
</div>

<!-- §1 EXECUTIVE SUMMARY -->
<h2>1. Executive Summary</h2>
<p>{{ report['verdict_text'] }}</p>
<h3>Flip-conditions (tornado zero-crossings)</h3>
<ul>
{% for fc in report['flip_conditions'] %}<li>{{ fc }}</li>{% endfor %}
</ul>

<!-- §2 SITE & CATCHMENT -->
<h2>2. Site & Catchment</h2>
<p>The site at 292-296 Johnston St, Abbotsford is geocoded from the exact address [1]. Catchment buffers of 1/3/5 km are constructed in EPSG:7855 (GDA2020/MGA zone 55) and population is area-apportioned across SA1 boundaries — fixing v1's whole-postcode summing flaw [2].</p>

<!-- §3 COMPETITOR LANDSCAPE -->
<h2>3. Competitor Landscape</h2>
<p>Competitors (GP clinics, medical centres, pharmacies, allied health) are mapped via Google Places API (New) with per-type/per-ring splitting and place_id dedupe [3]. Brand classification and clinic-vs-practitioner filtering are applied.</p>

<!-- §4 FINANCIAL MODEL -->
<h2>4. Financial Model</h2>
<p>The clinic P&L is modelled as a pure function of the parameters dict (ARCHITECTURE.md Pattern 3). Practice revenue = GP billings × service-fee % (Pitfall 11 guardrail — GP payments are NOT a cost line). The 36-month ramp-up cash flow models GP recruitment milestones and per-GP book-fill utilisation.</p>
{% if figs['ramp_cashflow'] %}<img src="{{ figs['ramp_cashflow'] }}"/>{% endif %}

<!-- §5 SCENARIOS & SENSITIVITY -->
<h2>5. Scenarios & Sensitivity</h2>
<p>Base/optimistic/pessimistic scenarios are run as override dicts on the same P&L function (FIN-04). Two tornado charts rank which assumptions move annual EBITDA and peak capital most (FIN-05). A billing-mix sensitivity curve compares 70/30 mixed vs 100%-bulk+BBPIP (FIN-06).</p>
{% if figs['tornado_ebitda'] %}<img src="{{ figs['tornado_ebitda'] }}"/>{% endif %}
{% if figs['tornado_peak_capital'] %}<img src="{{ figs['tornado_peak_capital'] }}"/>{% endif %}
{% if figs['billing_mix_curve'] %}<img src="{{ figs['billing_mix_curve'] }}"/>{% endif %}

<!-- §6 PHARMACY SYNERGY (SECONDARY UPSIDE — D-04) -->
<h2>6. Pharmacy Synergy — Secondary Upside (NOT Load-Bearing)</h2>
<div class="secondary">
<p><strong>The clinic verdict above is 100% standalone.</strong> The pharmacy synergy below is secondary upside, quantified only after the standalone verdict. It is NOT load-bearing — the clinic does not need the pharmacy to be profitable.</p>
{% set ps = report['pharmacy_synergy_range'] %}
<p>Catchment pop: {{ '{:,.0f}'.format(ps['catchment_pop']) }} · Scripts/person/yr: {{ ps['avg_scripts_per_person_yr'] }} · Gross margin/script: ${{ ps['gross_margin_per_script'] }}</p>
<ul>
{% for label, r in ps['results'].items() %}
<li><strong>{{ label|upper }}</strong> capture {{ '%.0f%%'|format(r['capture_rate']*100) }} → {{ '{:,.0f}'.format(r['annual_scripts']) }} scripts/yr → ${{ '{:,.0f}'.format(r['annual_profit']) }}/yr</li>
{% endfor %}
</ul>
</div>

<!-- §7 ASSUMPTIONS REGISTER (REP-01, D-16) -->
<h2>7. Assumptions Register</h2>
<table>
<tr><th>Parameter</th><th>Value</th><th>Source</th><th>Date</th><th>Confidence</th></tr>
{% for _, row in report['assumptions_register'].iterrows() %}
<tr><td>{{ row['Parameter'] }}</td><td>{{ row['Value'] }}</td><td>{{ row['Source'] }}</td><td>{{ row['Date'] }}</td><td>{{ row['Confidence'] }}</td></tr>
{% endfor %}
</table>

<!-- §8 DATA SOURCES & CITATIONS (REP-04, D-17) -->
<h2>8. Data Sources & Citations</h2>
<div class="footnote">
<p>[1] Google Geocoding API — site address geocoding. Access date: {{ report_date }}.</p>
<p>[2] ABS Census 2021 — G01/G02/G04 via ABS Data API (data.api.abs.gov.au/rest). Access date: {{ report_date }}.</p>
<p>[3] Google Places API (New) — Nearby Search. Access date: {{ report_date }}.</p>
<p>[4] MBS Online — item 23 rebate $43.90, from 1 Jul 2025 (2.4% indexation). Access date: {{ report_date }}.</p>
<p>[5] Fair Work — MA000034/MA000002, from 1 Jul 2025. Access date: {{ report_date }}.</p>
<p>[6] PBS Statistics 2023-24 — scripts/person/yr benchmark. Access date: {{ report_date }}.</p>
<p>[7] AIHW (2026) — Medicare-subsidised GP and allied health attendances, SA3 age-band rates.</p>
<p>[8] AMWAC 2000 — GP:population planning benchmark (1:905).</p>
</div>

</body>
</html>
"""

html_rendered = Template(report_html_template).render(
    report=report,
    figs=figs,
    report_date=_date.today().isoformat(),
)
(OUTPUTS / "Johnston_St_v2_Executive_Report.html").write_text(html_rendered, encoding="utf-8")
print(f"[§8.3] HTML report written to outputs/Johnston_St_v2_Executive_Report.html ({len(html_rendered):,} chars)")
```

**Cell 5 — Code: weasyprint PDF generation (REP-03, D-18):**
Colab-only. Detect Colab via `"google.colab" in sys.modules`. If Colab: `!pip install -q weasyprint` + `!apt-get install -y libpango-1.0-0 libpangocairo-1.0-0` (one line, per STACK.md), then `weasyprint.HTML(string=html_rendered).write_pdf(OUTPUTS / "Johnston_St_v2_Executive_Report.pdf")`. If local Windows: print a note "Open in Colab to generate the PDF" and skip (D-18). Wrap in try/except so a weasyprint failure doesn't crash the notebook (no-hard-fail principle, PIPE-04).

```python
# §8.4 PDF generation via weasyprint (REP-03, D-18)
# Colab-only — local Windows produces HTML only (D-18, STACK.md).
import sys

IS_COLAB = "google.colab" in sys.modules
pdf_path = OUTPUTS / "Johnston_St_v2_Executive_Report.pdf"

if IS_COLAB:
    try:
        import subprocess
        subprocess.run(["pip", "install", "-q", "weasyprint"], check=True)
        subprocess.run(["apt-get", "install", "-y", "libpango-1.0-0", "libpangocairo-1.0-0"], check=True)
        from weasyprint import HTML
        HTML(string=html_rendered).write_pdf(str(pdf_path))
        print(f"[§8.4] PDF written to {pdf_path} (Colab + weasyprint)")
    except Exception as e:
        print(f"[§8.4] weasyprint PDF generation failed: {e}")
        print(f"[§8.4] HTML report is available at outputs/Johnston_St_v2_Executive_Report.html")
else:
    print("[§8.4] Local Windows detected — HTML report produced (PDF generation is Colab-only, D-18).")
    print(f"[§8.4] Open in Colab to generate the PDF, or view the HTML: outputs/Johnston_St_v2_Executive_Report.html")
```

**Cell 6 — Markdown: §8 interpretation + final Next Steps:**
`## §8.5 Interpretation` — teaching commentary: §8 is the investor deliverable. The verdict is binary and standalone (D-03, D-04); the assumptions register (REP-01) and citations (REP-04) are the credibility layer; the PDF is Colab-only because weasyprint's GTK deps are painful on Windows (D-18). Re-running §8 alone regenerates the report from the current `report{}` state — the notebook is the single source of truth. This completes the feasibility study: the notebook + PDF answer "can the clinic be profitable on its own at this site?" with defensible data and transparent assumptions.

Then a final `## Next Steps` markdown: "Feasibility study complete. The notebook + PDF are the deliverable. Deferred to a future milestone: Monte Carlo confidence intervals (V2-01), drive-time isochrone catchments (V2-02), interactive dashboard."

After updating the generator, RUN IT: `python scripts/extend_v2_notebook_phase5.py` to extend the notebook with §7+§8.
  </action>
  <verify>
    python scripts/extend_v2_notebook_phase5.py  # must run without error
    python -c "import json,sys; sys.stdout.reconfigure(encoding='utf-8'); nb=json.load(open('Johnston_St_v2.ipynb',encoding='utf-8')); s=''.join(''.join(c.get('source',[])) for c in nb['cells']); assert '# §8 Executive Report' in s, 'No §8 header'; assert 'verdict = ' in s, 'No verdict variable'; assert '\"GO\"' in s and '\"NO-GO\"' in s, 'No binary verdict (D-03)'; assert 'GATE_MARGIN' in s, 'No margin gate (D-01)'; assert '0.15' in s, 'No 15% margin threshold (D-01)'; assert 'verdict_text' in s, 'No verdict_text'; assert 'flip_conditions' in s, 'No flip-conditions (D-02)'; assert 'tornado_zero_crossings' in s, 'No tornado zero-crossings reference'; assert 'key_numbers' in s, 'No key_numbers (D-13)'; assert 'assumptions_register' in s, 'No assumptions register (REP-01)'; assert 'Parameter' in s and 'Source' in s and 'Date' in s and 'Confidence' in s, 'Missing 5-column register headers (D-16)'; assert 'jinja2' in s or 'Jinja2' in s or 'Template' in s, 'No Jinja2 template (D-15)'; assert 'weasyprint' in s, 'No weasyprint (REP-03)'; assert 'google.colab' in s, 'No Colab detection (D-18)'; assert 'Johnston_St_v2_Executive_Report.html' in s, 'No HTML output path'; assert 'Johnston_St_v2_Executive_Report.pdf' in s, 'No PDF output path'; assert 'base64' in s, 'No base64 image embedding'; assert 'footnote' in s, 'No footnote citations (D-17)'; assert 'Executive Summary' in s, 'Missing §1 Executive Summary'; assert 'Site & Catchment' in s, 'Missing §2 Site & Catchment'; assert 'Competitor Landscape' in s, 'Missing §3 Competitor Landscape'; assert 'Financial Model' in s, 'Missing §4 Financial Model'; assert 'Scenarios & Sensitivity' in s, 'Missing §5 Scenarios & Sensitivity'; assert 'Pharmacy Synergy' in s, 'Missing §6 Pharmacy Synergy'; assert 'Assumptions Register' in s, 'Missing §7 Assumptions Register'; assert 'Data Sources' in s or 'Citations' in s, 'Missing §8 Data Sources & Citations'; assert 'Secondary Upside' in s or 'secondary upside' in s, 'No secondary-upside label in §8'; assert 'NOT Load-Bearing' in s or 'not load-bearing' in s or 'NOT load-bearing' in s, 'No not-load-bearing label in §8'; assert 'report[\"verdict\"]' in s or \"report['verdict']\" in s, 'No verdict deposit'; assert 'report[\"assumptions_register\"]' in s or \"report['assumptions_register']\" in s, 'No assumptions_register deposit'; assert 'template.render' in s or 'Template(' in s, 'No Jinja2 render call'; print('All checks passed')"
  </verify>
  <acceptance_criteria>
    - scripts/extend_v2_notebook_phase5.py runs without error (`python scripts/extend_v2_notebook_phase5.py` exits 0)
    - Johnston_St_v2.ipynb contains "# §8 Executive Report" section header
    - Johnston_St_v2.ipynb contains a binary GO/NO-GO verdict (D-03) gated on EBITDA > 0 AND margin > 15% (D-01)
    - Johnston_St_v2.ipynb contains verdict_text that NEVER mentions the pharmacy (D-04 — verify by string inspection)
    - Johnston_St_v2.ipynb contains flip-conditions derived from tornado_zero_crossings (D-02)
    - Johnston_St_v2.ipynb contains 5 key_numbers: steady_state_ebitda, steady_state_margin, peak_capital, breakeven_operating, breakeven_payback (D-13)
    - Johnston_St_v2.ipynb contains a 5-column assumptions register (Parameter | Value | Source | Date | Confidence) built from BASE_ASSUMPTIONS (REP-01, D-16)
    - Johnston_St_v2.ipynb contains a Jinja2 HTML template with all 8 sections (D-14): Executive Summary, Site & Catchment, Competitor Landscape, Financial Model, Scenarios & Sensitivity, Pharmacy Synergy, Assumptions Register, Data Sources & Citations
    - Johnston_St_v2.ipynb embeds static PNG figures (catchment_3km, ramp_cashflow, tornado_ebitda, tornado_peak_capital, billing_mix_curve) as base64 — no folium (D-15, Pitfall 16)
    - Johnston_St_v2.ipynb contains footnote-style citations [1], [2] with access dates (D-17, REP-04)
    - Johnston_St_v2.ipynb contains weasyprint PDF generation gated on Colab detection (REP-03, D-18)
    - Johnston_St_v2.ipynb writes outputs/Johnston_St_v2_Executive_Report.html (always) and .pdf (Colab-only)
    - Johnston_St_v2.ipynb deposits verdict, key_numbers, flip_conditions, assumptions_register into report{} (Pattern 5)
    - The §8 render step is re-runnable standalone (reads report{} + saved PNGs only — no §6/§7 re-execution required)
    - scripts/extend_v2_notebook_phase5.py is idempotent (re-running regenerates §7+§8 from the "# §7 Scenarios & Sensitivity" marker)
    - weasyprint failure does not crash the notebook (try/except, no-hard-fail principle PIPE-04)
  </acceptance_criteria>
</task>

</tasks>
